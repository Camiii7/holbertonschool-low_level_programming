#include "main.h"

/**
 * _abs - Calcula el valor absoluto de un entero.
 * @n: El entero a verificar.
 * Return: El valor absoluto del entero.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
		return (n);
}
